orchestra.config.json
dispatch.log.jsonl
resume.schedule.json
devices.json
devices.json.tmp
audit.log.jsonl
events.log.json
events.log.json.tmp
idem.store.json
idem.store.json.tmp
__pycache__/
# the generated hook fragment + script (orchestra.hooks.install). Regenerated on
# every dispatch, and it carries THIS machine's port — committing it would ship
# somebody else's port to everyone else's board.
.orchestra/
*.log
nohup.out
.DS_Store

# APNs auth keys — never commit a private key
*.p8
.orchestra/
ios/Signing.xcconfig

# per-boot pairing lockfile — runtime state, like devices.json
devices.lock
