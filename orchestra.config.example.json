{
  "_readme": "Copy this to orchestra.config.json (gitignored) and fill in your values. Every key is optional; delete what you do not use. The apns_* keys are only needed for push to the iOS app — see docs/mobile/APNS-SETUP.md.",

  "roots": ["/Users/YOU/code"],
  "pattern": "",
  "exclude_accounts": [],
  "reserve_percent": {"main": 20},
  "router_home": null,

  "_disk": "orchestra REPORTS what ~/.claude*/projects costs and never deletes any of it — retention there is yours. log_max_mb/log_keep govern ONLY the two logs orchestra writes itself (audit.log.jsonl, dispatch.log.jsonl); a rotated segment under 7 days old is never removed whatever log_keep says. `--disk-report` prints the report, `--prune-logs` runs the reap. Set disk_report_h to 0 to turn the reporting thread off.",
  "disk_report_h": 6.0,
  "disk_warn_gb": 10.0,
  "disk_free_gb": 10.0,
  "log_max_mb": 8.0,
  "log_keep": 5,

  "_uploads": "POST /api/v1/uploads writes an image from the phone to ~/.orchestra/uploads/<YYYY-MM-DD>/ (0700 dirs, 0600 files, outside every git worktree on purpose) and answers with its absolute path, which the app pastes into the message — a path is the only way to hand an agent a picture. PNG/JPEG/GIF/WebP/HEIC only, sniffed from the bytes; the server names the file. These are orchestra's OWN files, so unlike your transcripts they are pruned: upload_retain_days, with a hard 24-hour floor that outranks any smaller value. Set upload_retain_days to 0 to keep them forever.",
  "upload_max_mb": 10.0,
  "upload_retain_days": 30,

  "apns_team_id": "XXXXXXXXXX",
  "apns_topic": "sh.orchestra.app",
  "apns_environment": "sandbox",
  "apns_key_id": "XXXXXXXXXX",
  "apns_key_path": "/Users/YOU/.orchestra/apns/AuthKey_XXXXXXXXXX.p8"
}
